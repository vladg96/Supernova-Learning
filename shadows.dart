import 'package:flutter/material.dart';

class AppShadows {
  static const shad1 = BoxShadow(
    color: Color(0xff000000),
    offset: Offset(2, 2),
    blurRadius: 0,
    spreadRadius: 0,
  );

  AppShadows._();
}
